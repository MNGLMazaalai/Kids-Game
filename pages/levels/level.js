import path from 'path'
// import fs from 'fs'
// const postDirectory = path.join(process.cwd(), 'pages/levels')

export default function Hello() {
    // const levelname = fs.readdirSync(postDirectory)
    // console.log(levelname)
    return <div> {levelname}</div>
}